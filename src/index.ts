/**
 * @amlplugins/hubspot-crm-companies
 *
 * Thin re-export of @hubspot/api-client (CommonJS `export =` SDK).
 *
 * HubSpot CRM Companies — company properties, associations, batch CRUD, search.
 */

import _sdk from "@hubspot/api-client";
export { _sdk as sdk };
export default _sdk;
